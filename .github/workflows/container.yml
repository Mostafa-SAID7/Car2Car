name: Container

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: container-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  image:
    name: Build and health-check image
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Build image
        run: docker build --file docker/Dockerfile --tag motor-oil-eg:test .

      - name: Start container
        run: docker run --detach --name motor-oil-eg-test --publish 18080:8080 motor-oil-eg:test

      - name: Check health endpoint
        run: |
          for attempt in {1..20}; do
            if curl --fail --silent http://127.0.0.1:18080/healthz; then
              exit 0
            fi
            sleep 2
          done
          docker logs motor-oil-eg-test
          exit 1

      - name: Check SPA fallback
        run: curl --fail --silent http://127.0.0.1:18080/catalog >/dev/null

      - name: Stop container
        if: always()
        run: docker stop motor-oil-eg-test || true
